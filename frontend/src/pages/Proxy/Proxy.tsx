import { Button } from 'flowbite-react'

export default function Proxy() {
    return (
        <div id="networkPage" className="h-screen w-screen">
            <Button gradientDuoTone="greenToBlue">I am the Proxy Page</Button>
        </div>
    )
}